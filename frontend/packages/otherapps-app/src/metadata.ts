import { type Metadata } from "@portal-core/types";

import { AppIcon } from "./App";

const appMedadata: Metadata = {
  title: "アプリ一覧",
  description: "",
  AppIcon: AppIcon,
};

export { appMedadata };
