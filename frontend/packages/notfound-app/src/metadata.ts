import { type Metadata } from "@packages/portal-core";

import { AppIcon } from "./App";

const appMedadata: Metadata = {
  title: "不明なアプリ",
  description: "",
  AppIcon: AppIcon,
};

export { appMedadata };
